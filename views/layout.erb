<!DOCTYPE html>
<html>
	<head>
	  <title>Movable Ink Zuora Utilities</title>
	  <meta content="text/html; charset=utf-8" http-equiv="Content-Type">
	  <meta name="viewport" content="width=device-width, initial-scale=1.0">

		<link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
	  <link href="/stylesheets/style.css" rel="stylesheet">
	  <link href="/images/logo_mi.png" rel="shortcut icon">
	</head>
	<body>
		<div class="container" style='padding-top: 50px;'>
		  <table>
		    <tbody>
			    <tr>
			      <td style="width:30%;">
			        <a href='/' > <img src="/images/logo_mi_full.png"> </a>
			      </td>
			      <td>
			        <h1>Movable Ink Zuora Utilities</h1>
			        <%= yield %>
			      </td>
			    </tr>
		  	</tbody>
	  	</table>
	  	<table class="table table-striped table-bordered bootstrap-datatable datatable">
        <thead>
          <tr>
            <th>Date</th>
            <th>Output Update File</th>
          </tr>
        </thead>   
        <tbody>
          <% Dir["*.csv"].sort.reverse.each do |file| %>
            <tr>
              <td  class="center"><%= Time.at(file.split('-').last.split('.csv').first.to_i).strftime('%m/%d/%y %H:%M') %></td>
              <td  class="center" data-id="<%=file %>">
              	<a href='/download/<%= file %>' ><%= file %></a>
              </td>
          	</tr>
          <% end %>
        </tbody>
      </table>      
	  </div>
	   <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="/vendor/jquery-1.9.1.min.js"></script>
    <script src="/vendor/bootstrap/js/bootstrap.min.js"></script>
    <script src="/vendor/jquery.validate.js"></script>
    <script src="/vendor/jquery.dataTables.js"></script>
    <script src="/vendor/dataTables.bootstrap.js"></script>

    <script type="text/javascript">		
		  $(document).ready(function(){
		  	$('#rolling_window_submit_btn').on('click', function(e){
		  		$(this).attr("disabled", true);
		  		$(this).text('Processing...');
		  		$(this).val('Processing...');
		  		$('#rolling_window_form').submit();
		  	})

		  	<% if @download_last == true %>
				$('table').eq(1).find('td').eq(1).children()[0].click()
			<% end %>

		  })
		</script>
	</body>
</html>


        
